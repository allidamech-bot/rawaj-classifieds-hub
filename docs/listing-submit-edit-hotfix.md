# Listing submit/edit production hotfix

This hotfix makes listing content edits and draft/rejected submission self-contained RPC operations. It removes write-path dependence on reference-data loading and direct pending-review table updates.
