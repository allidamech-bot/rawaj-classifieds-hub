/**
 * Backward-compatible module name. Taxonomy metadata is Cloudflare-only now,
 * so this file is intentionally a pure re-export with no provider branching.
 */
export * from "./taxonomy-metadata";
