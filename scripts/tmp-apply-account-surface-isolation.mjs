import { readFile, writeFile } from "node:fs/promises";

async function patchFile(path, transform) {
  const source = await readFile(path, "utf8");
  const next = transform(source);
  if (next === source) throw new Error(`No changes applied to ${path}`);
  await writeFile(path, next);
}

function replaceExact(source, before, after, label) {
  const first = source.indexOf(before);
  if (first < 0) throw new Error(`Missing ${label}`);
  if (source.indexOf(before, first + before.length) >= 0) throw new Error(`Duplicate ${label}`);
  return source.slice(0, first) + after + source.slice(first + before.length);
}

function replaceCount(source, before, after, expected, label) {
  const count = source.split(before).length - 1;
  if (count !== expected) throw new Error(`Expected ${expected} ${label}, found ${count}`);
  return source.split(before).join(after);
}

await patchFile("src/routes/favorites.tsx", (source) => {
  source = replaceExact(
    source,
    `  const removeInFlightRef = useRef<Set<string>>(new Set());\n  const loadRequestIdRef = useRef(0);\n  const profileId = auth.profile?.id ?? null;`,
    `  const removeInFlightRef = useRef<Set<string>>(new Set());\n  const loadRequestIdRef = useRef(0);\n  const profileId = auth.profile?.id ?? null;\n  const profileIdRef = useRef<string | null>(profileId);\n  profileIdRef.current = profileId;`,
    "favorites live profile ref",
  );
  source = replaceCount(
    source,
    "currentProfileId !== auth.profile?.id",
    "currentProfileId !== profileIdRef.current",
    2,
    "favorites live account guards",
  );
  source = replaceExact(
    source,
    `  async function remove(listingId: string) {\n    if (removeInFlightRef.current.has(listingId)) return;\n    const currentProfileId = profileId;\n    removeInFlightRef.current.add(listingId);\n    setActionMessage(\"\");\n    try {\n      const result = await unfavoriteListing(currentProfileId, listingId);\n      if (currentProfileId !== profileIdRef.current) return;\n      if (!result.ok) {\n        setActionMessage(result.error.message);\n        return;\n      }\n      setItems((current) => current.filter((item) => item.listingId !== listingId));\n    } finally {\n      removeInFlightRef.current.delete(listingId);\n    }\n  }`,
    `  async function remove(listingId: string) {\n    const currentProfileId = profileId;\n    if (!currentProfileId) return;\n    const scopeKey = [currentProfileId, listingId].join(\":\");\n    if (removeInFlightRef.current.has(scopeKey)) return;\n\n    removeInFlightRef.current.add(scopeKey);\n    setActionMessage(\"\");\n    try {\n      const result = await unfavoriteListing(currentProfileId, listingId);\n      if (currentProfileId !== profileIdRef.current) return;\n      if (!result.ok) {\n        setActionMessage(result.error.message);\n        return;\n      }\n      setItems((current) => current.filter((item) => item.listingId !== listingId));\n    } finally {\n      removeInFlightRef.current.delete(scopeKey);\n    }\n  }`,
    "favorites scoped remove action",
  );
  return source;
});

await patchFile("src/routes/saved-searches.tsx", (source) => {
  source = replaceExact(
    source,
    `  const [scanMessage, setScanMessage] = useState(\"\");\n  const loadRequestIdRef = useRef(0);\n  const profileId = auth.profile?.id ?? null;`,
    `  const [scanMessage, setScanMessage] = useState(\"\");\n  const loadRequestIdRef = useRef(0);\n  const loadedProfileIdRef = useRef<string | null>(null);\n  const profileId = auth.profile?.id ?? null;\n  const profileIdRef = useRef<string | null>(profileId);\n  const creatingSearchProfilesRef = useRef<Set<string>>(new Set());\n  const frequencyScopesRef = useRef<Set<string>>(new Set());\n  const deletingSearchScopesRef = useRef<Set<string>>(new Set());\n  profileIdRef.current = profileId;`,
    "saved searches action refs",
  );
  source = replaceCount(
    source,
    "currentProfileId !== auth.profile?.id",
    "currentProfileId !== profileIdRef.current",
    3,
    "saved search live account guards",
  );
  source = replaceExact(
    source,
    `  useEffect(() => {\n    if (auth.status !== \"signedIn\" || !profileId) {\n      loadRequestIdRef.current += 1;\n      setItems([]);\n      setLoading(false);\n      setHasLoaded(false);\n      setLoadError(null);\n      setMessage(\"\");\n      setScanMessage(\"\");\n      return;\n    }\n\n    loadRequestIdRef.current += 1;\n    setItems([]);\n    setLoading(false);\n    setHasLoaded(false);\n    setLoadError(null);\n    setMessage(\"\");\n    setScanMessage(\"\");\n    void loadSavedSearches();\n\n    return () => {\n      loadRequestIdRef.current += 1;\n    };\n  }, [auth.status, loadSavedSearches, profileId]);`,
    `  useEffect(() => {\n    if (auth.status !== \"signedIn\" || !profileId) {\n      loadRequestIdRef.current += 1;\n      loadedProfileIdRef.current = null;\n      setItems([]);\n      setLocalItems([]);\n      setLoading(false);\n      setHasLoaded(false);\n      setLoadError(null);\n      setName(\"\");\n      setKeyword(\"\");\n      setFrequency(\"weekly\");\n      setSavingFrequencyId(null);\n      setMessage(\"\");\n      setScanMessage(\"\");\n      return;\n    }\n\n    const profileChanged = loadedProfileIdRef.current !== profileId;\n    loadedProfileIdRef.current = profileId;\n    loadRequestIdRef.current += 1;\n    setItems([]);\n    setLoading(false);\n    setHasLoaded(false);\n    setLoadError(null);\n    setMessage(\"\");\n    setScanMessage(\"\");\n    if (profileChanged) {\n      setLocalItems([]);\n      setName(\"\");\n      setKeyword(\"\");\n      setFrequency(\"weekly\");\n      setSavingFrequencyId(null);\n    }\n    void loadSavedSearches();\n\n    return () => {\n      loadRequestIdRef.current += 1;\n    };\n  }, [auth.status, loadSavedSearches, profileId]);`,
    "saved search account reset",
  );
  source = replaceExact(
    source,
    `  async function addSavedSearch(event: FormEvent) {\n    event.preventDefault();\n    setMessage(\"\");\n    const label = name.trim() || text(\"بحث محفوظ\", \"Saved search\");\n    const filters = buildListingFilters();\n    const result = await createSavedSearch(profileId, {\n      nameAr: label,\n      filters,\n      alertFrequency: frequency,\n    });\n\n    if (result.ok) {\n      setItems((current) => [result.data, ...current]);\n      setHasLoaded(true);\n      setMessage(text(\"تم حفظ البحث في حسابك.\", \"Search saved to your account.\"));\n    } else {\n      setLocalItems((current) => [\n        {\n          id: \`local-\${Date.now()}\`,\n          nameAr: label,\n          filters: toLocalFilters(filters),\n          createdAt: new Date().toISOString(),\n          frequency,\n        },\n        ...current,\n      ]);\n      setMessage(\n        text(\n          \"تعذر حفظ البحث في الحساب، فتم حفظه لهذه الجلسة فقط.\",\n          \"Could not save this search to the account, so it was saved for this session only.\",\n        ),\n      );\n    }\n    setName(\"\");\n    setKeyword(\"\");\n  }`,
    `  async function addSavedSearch(event: FormEvent) {\n    event.preventDefault();\n    const currentProfileId = profileId;\n    if (!currentProfileId || creatingSearchProfilesRef.current.has(currentProfileId)) return;\n\n    const label = name.trim() || text(\"بحث محفوظ\", \"Saved search\");\n    const filters = buildListingFilters();\n    const currentFrequency = frequency;\n    creatingSearchProfilesRef.current.add(currentProfileId);\n    setMessage(\"\");\n    try {\n      const result = await createSavedSearch(currentProfileId, {\n        nameAr: label,\n        filters,\n        alertFrequency: currentFrequency,\n      });\n      if (currentProfileId !== profileIdRef.current) return;\n\n      if (result.ok) {\n        setItems((current) => [result.data, ...current]);\n        setHasLoaded(true);\n        setMessage(text(\"تم حفظ البحث في حسابك.\", \"Search saved to your account.\"));\n      } else {\n        setLocalItems((current) => [\n          {\n            id: \`local-\${currentProfileId}-\${Date.now()}\`,\n            nameAr: label,\n            filters: toLocalFilters(filters),\n            createdAt: new Date().toISOString(),\n            frequency: currentFrequency,\n          },\n          ...current,\n        ]);\n        setMessage(\n          text(\n            \"تعذر حفظ البحث في الحساب، فتم حفظه لهذه الجلسة فقط.\",\n            \"Could not save this search to the account, so it was saved for this session only.\",\n          ),\n        );\n      }\n      setName(\"\");\n      setKeyword(\"\");\n    } finally {\n      creatingSearchProfilesRef.current.delete(currentProfileId);\n    }\n  }`,
    "saved search create action",
  );
  source = replaceExact(
    source,
    `  async function changeAlertFrequency(id: string, next: SavedSearchAlertFrequency) {\n    setSavingFrequencyId(id);\n    setMessage(\"\");\n    const previous = items;\n    setItems((current) =>\n      current.map((item) => (item.id === id ? { ...item, alertFrequency: next } : item)),\n    );\n    const result = await updateSavedSearchAlertFrequency(profileId, id, next);\n    setSavingFrequencyId(null);\n    if (!result.ok) {\n      setItems(previous);\n      setMessage(result.error.message);\n      return;\n    }\n    setItems((current) => current.map((item) => (item.id === id ? result.data : item)));\n    setMessage(text(\"تم تحديث تكرار التنبيه.\", \"Alert frequency updated.\"));\n  }`,
    `  async function changeAlertFrequency(id: string, next: SavedSearchAlertFrequency) {\n    const currentProfileId = profileId;\n    if (!currentProfileId) return;\n    const scopeKey = [currentProfileId, id].join(\":\");\n    if (frequencyScopesRef.current.has(scopeKey)) return;\n\n    const previous = items;\n    frequencyScopesRef.current.add(scopeKey);\n    setSavingFrequencyId(id);\n    setMessage(\"\");\n    setItems((current) =>\n      current.map((item) => (item.id === id ? { ...item, alertFrequency: next } : item)),\n    );\n    try {\n      const result = await updateSavedSearchAlertFrequency(currentProfileId, id, next);\n      if (currentProfileId !== profileIdRef.current) return;\n      if (!result.ok) {\n        setItems(previous);\n        setMessage(result.error.message);\n        return;\n      }\n      setItems((current) => current.map((item) => (item.id === id ? result.data : item)));\n      setMessage(text(\"تم تحديث تكرار التنبيه.\", \"Alert frequency updated.\"));\n    } finally {\n      frequencyScopesRef.current.delete(scopeKey);\n      if (currentProfileId === profileIdRef.current) setSavingFrequencyId(null);\n    }\n  }`,
    "saved search frequency action",
  );
  source = replaceExact(
    source,
    `  async function removeSavedSearch(id: string) {\n    setMessage(\"\");\n    const result = await deleteSavedSearch(profileId, id);\n    if (!result.ok) {\n      setMessage(result.error.message);\n      return;\n    }\n\n    setItems((current) => current.filter((item) => item.id !== id));\n    setMessage(text(\"تم حذف البحث المحفوظ.\", \"Saved search removed.\"));\n  }`,
    `  async function removeSavedSearch(id: string) {\n    const currentProfileId = profileId;\n    if (!currentProfileId) return;\n    const scopeKey = [currentProfileId, id].join(\":\");\n    if (deletingSearchScopesRef.current.has(scopeKey)) return;\n\n    deletingSearchScopesRef.current.add(scopeKey);\n    setMessage(\"\");\n    try {\n      const result = await deleteSavedSearch(currentProfileId, id);\n      if (currentProfileId !== profileIdRef.current) return;\n      if (!result.ok) {\n        setMessage(result.error.message);\n        return;\n      }\n\n      setItems((current) => current.filter((item) => item.id !== id));\n      setMessage(text(\"تم حذف البحث المحفوظ.\", \"Saved search removed.\"));\n    } finally {\n      deletingSearchScopesRef.current.delete(scopeKey);\n    }\n  }`,
    "saved search delete action",
  );
  source = replaceExact(source, "key={item.id}\n                    id={item.id}", "key={`${profileId ?? \"signed-out\"}:${item.id}`}\n                    id={item.id}", "local saved search key");
  source = replaceExact(source, "key={item.id}\n                    id={item.id}", "key={`${profileId ?? \"signed-out\"}:${item.id}`}\n                    id={item.id}", "server saved search key");
  return source;
});

await patchFile("src/routes/profile/listings.tsx", (source) => {
  source = replaceExact(
    source,
    `  const sellerRequestIdRef = useRef(0);\n  const profileId = auth.profile?.id ?? null;`,
    `  const sellerRequestIdRef = useRef(0);\n  const profileId = auth.profile?.id ?? null;\n  const profileIdRef = useRef<string | null>(profileId);\n  profileIdRef.current = profileId;`,
    "owner listings live profile ref",
  );
  source = replaceCount(source, "currentProfileId !== auth.profile?.id", "currentProfileId !== profileIdRef.current", 2, "owner listings load guards");
  source = replaceExact(
    source,
    `  function handleListingDeleted(listingId: string) {\n    setListings((prev) => prev.filter((listing) => listing.id !== listingId));\n  }\n\n  function handleListingChanged(nextListing: ClassifiedListing) {\n    setListings((prev) =>\n      prev.map((listing) => (listing.id === nextListing.id ? nextListing : listing)),\n    );\n  }`,
    `  function handleListingDeleted(actionProfileId: string | null, listingId: string) {\n    if (!actionProfileId || actionProfileId !== profileIdRef.current) return;\n    setListings((prev) => prev.filter((listing) => listing.id !== listingId));\n  }\n\n  function handleListingChanged(\n    actionProfileId: string | null,\n    nextListing: ClassifiedListing,\n  ) {\n    if (!actionProfileId || actionProfileId !== profileIdRef.current) return;\n    setListings((prev) =>\n      prev.map((listing) => (listing.id === nextListing.id ? nextListing : listing)),\n    );\n  }`,
    "owner listing result guards",
  );
  source = replaceExact(source, "key={listing.id}\n                    listing={listing}", "key={`${profileId ?? \"signed-out\"}:${listing.id}`}\n                    listing={listing}", "owner listing account key");
  source = replaceExact(source, "onDeleted: (id: string) => void;\n  onChanged: (listing: ClassifiedListing) => void;", "onDeleted: (profileId: string | null, id: string) => void;\n  onChanged: (profileId: string | null, listing: ClassifiedListing) => void;", "owner listing callback types");
  source = replaceExact(source, "onDeleted(listing.id);", "onDeleted(userId, listing.id);", "owner listing delete callback");
  source = replaceCount(source, "onChanged(result.data);", "onChanged(userId, result.data);", 5, "owner listing change callbacks");
  return source;
});

await patchFile("src/features/reviews/SellerReviewCard.tsx", (source) => {
  source = replaceExact(source, `import { useState, type FormEvent } from \"react\";`, `import { useEffect, useRef, useState, type FormEvent } from \"react\";`, "review card hook imports");
  source = replaceExact(
    source,
    `  const [reportNotice, setReportNotice] = useState(\"\");\n  const [reported, setReported] = useState(false);\n  const canReport =`,
    `  const [reportNotice, setReportNotice] = useState(\"\");\n  const [reported, setReported] = useState(false);\n  const profileId = auth.profile?.id ?? null;\n  const profileIdRef = useRef<string | null>(profileId);\n  const responseScopesRef = useRef<Set<string>>(new Set());\n  const reportScopesRef = useRef<Set<string>>(new Set());\n  profileIdRef.current = profileId;\n\n  useEffect(() => {\n    const response = readSellerReviewResponse(review);\n    setSavedResponse(response.sellerResponse);\n    setDraft(response.sellerResponse ?? \"\");\n    setSaving(false);\n    setNotice(\"\");\n    setReportOpen(false);\n    setReportReason(\"abuse\");\n    setReportDetails(\"\");\n    setReportSaving(false);\n    setReportNotice(\"\");\n    setReported(false);\n  }, [profileId, review]);\n\n  const canReport =`,
    "review card account reset",
  );
  source = replaceExact(
    source,
    `  async function persistResponse(responseText: string) {\n    if (saving) return;\n    setNotice(\"\");\n    setSaving(true);\n    const result = await setSellerReviewResponse(review.id, responseText);\n    setSaving(false);\n\n    if (!result.ok) {\n      setNotice(result.error.message);\n      return;\n    }\n\n    const response = readSellerReviewResponse(result.data);\n    setSavedResponse(response.sellerResponse);\n    setDraft(response.sellerResponse ?? \"\");\n    setNotice(\n      response.sellerResponse\n        ? text(\"تم حفظ رد البائع.\", \"Seller response saved.\")\n        : text(\"تم حذف رد البائع.\", \"Seller response removed.\"),\n    );\n  }`,
    `  async function persistResponse(responseText: string) {\n    const currentProfileId = profileId;\n    if (!currentProfileId || !canManageResponse) return;\n    const scopeKey = [currentProfileId, review.id].join(\":\");\n    if (responseScopesRef.current.has(scopeKey)) return;\n\n    responseScopesRef.current.add(scopeKey);\n    setNotice(\"\");\n    setSaving(true);\n    try {\n      const result = await setSellerReviewResponse(review.id, responseText);\n      if (currentProfileId !== profileIdRef.current) return;\n      if (!result.ok) {\n        setNotice(result.error.message);\n        return;\n      }\n\n      const response = readSellerReviewResponse(result.data);\n      setSavedResponse(response.sellerResponse);\n      setDraft(response.sellerResponse ?? \"\");\n      setNotice(\n        response.sellerResponse\n          ? text(\"تم حفظ رد البائع.\", \"Seller response saved.\")\n          : text(\"تم حذف رد البائع.\", \"Seller response removed.\"),\n      );\n    } finally {\n      responseScopesRef.current.delete(scopeKey);\n      if (currentProfileId === profileIdRef.current) setSaving(false);\n    }\n  }`,
    "review response isolation",
  );
  source = replaceExact(
    source,
    `  async function submitReport(event: FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    if (!canReport || reportSaving || reported) return;\n\n    setReportNotice(\"\");\n    setReportSaving(true);\n    const result = await createSellerReviewReport(review.id, reportReason, reportDetails);\n    setReportSaving(false);\n\n    if (!result.ok) {\n      setReportNotice(result.error.message);\n      return;\n    }\n\n    setReported(true);\n    setReportOpen(false);\n    setReportDetails(\"\");\n    setReportNotice(\n      text(\n        \"تم إرسال البلاغ للمراجعة دون إخفاء التقييم تلقائيا.\",\n        \"Report submitted for review without automatically hiding the review.\",\n      ),\n    );\n  }`,
    `  async function submitReport(event: FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    const currentProfileId = profileId;\n    if (!currentProfileId || !canReport || reported) return;\n    const scopeKey = [currentProfileId, review.id].join(\":\");\n    if (reportScopesRef.current.has(scopeKey)) return;\n\n    reportScopesRef.current.add(scopeKey);\n    setReportNotice(\"\");\n    setReportSaving(true);\n    try {\n      const result = await createSellerReviewReport(review.id, reportReason, reportDetails);\n      if (currentProfileId !== profileIdRef.current) return;\n      if (!result.ok) {\n        setReportNotice(result.error.message);\n        return;\n      }\n\n      setReported(true);\n      setReportOpen(false);\n      setReportDetails(\"\");\n      setReportNotice(\n        text(\n          \"تم إرسال البلاغ للمراجعة دون إخفاء التقييم تلقائيا.\",\n          \"Report submitted for review without automatically hiding the review.\",\n        ),\n      );\n    } finally {\n      reportScopesRef.current.delete(scopeKey);\n      if (currentProfileId === profileIdRef.current) setReportSaving(false);\n    }\n  }`,
    "review report isolation",
  );
  return source;
});

await patchFile("src/routes/seller.$id.tsx", (source) => {
  source = replaceExact(
    source,
    `  const eligibilityRequestIdRef = useRef(0);\n  const reviewInFlightRef = useRef(false);\n  const isOwnProfile = auth.status === \"signedIn\" && auth.profile?.id === seller.id;\n  const shouldCheckEligibility = auth.status === \"signedIn\" && !isOwnProfile;`,
    `  const eligibilityRequestIdRef = useRef(0);\n  const profileId = auth.profile?.id ?? null;\n  const profileIdRef = useRef<string | null>(profileId);\n  const reviewSubmitProfilesRef = useRef<Set<string>>(new Set());\n  profileIdRef.current = profileId;\n  const isOwnProfile = auth.status === \"signedIn\" && profileId === seller.id;\n  const shouldCheckEligibility = auth.status === \"signedIn\" && Boolean(profileId) && !isOwnProfile;\n\n  useEffect(() => {\n    eligibilityRequestIdRef.current += 1;\n    setRating(5);\n    setComment(\"\");\n    setSelectedTraits([]);\n    setNotice(\"\");\n    setSaving(false);\n    setEligibilityState(\"idle\");\n  }, [profileId, seller.id]);`,
    "seller review account refs",
  );
  source = replaceExact(
    source,
    `  const loadEligibility = useCallback(async () => {\n    if (!shouldCheckEligibility) {\n      eligibilityRequestIdRef.current += 1;\n      setEligibilityState(\"idle\");\n      return;\n    }\n    const requestId = ++eligibilityRequestIdRef.current;\n    setEligibilityState(\"loading\");\n    setNotice(\"\");\n    const result = await fetchSellerReviewEligibility(seller.id);\n    if (requestId !== eligibilityRequestIdRef.current) return;\n    if (!result.ok) {\n      setEligibilityState(\"error\");\n      return;\n    }\n    if (result.data.eligible) {\n      setEligibilityState(\"eligible\");\n      return;\n    }\n    if (result.data.reason === \"existing_review\") {\n      setEligibilityState(\"existing_review\");\n      return;\n    }\n    if (result.data.reason === \"no_qualifying_interaction\") {\n      setEligibilityState(\"no_qualifying_interaction\");\n      return;\n    }\n    setEligibilityState(\"error\");\n  }, [seller.id, shouldCheckEligibility]);`,
    `  const loadEligibility = useCallback(async () => {\n    const currentProfileId = profileId;\n    if (!shouldCheckEligibility || !currentProfileId) {\n      eligibilityRequestIdRef.current += 1;\n      setEligibilityState(\"idle\");\n      return;\n    }\n    const requestId = ++eligibilityRequestIdRef.current;\n    setEligibilityState(\"loading\");\n    setNotice(\"\");\n    const result = await fetchSellerReviewEligibility(seller.id);\n    if (\n      requestId !== eligibilityRequestIdRef.current ||\n      currentProfileId !== profileIdRef.current\n    )\n      return;\n    if (!result.ok) {\n      setEligibilityState(\"error\");\n      return;\n    }\n    if (result.data.eligible) {\n      setEligibilityState(\"eligible\");\n      return;\n    }\n    if (result.data.reason === \"existing_review\") {\n      setEligibilityState(\"existing_review\");\n      return;\n    }\n    if (result.data.reason === \"no_qualifying_interaction\") {\n      setEligibilityState(\"no_qualifying_interaction\");\n      return;\n    }\n    setEligibilityState(\"error\");\n  }, [profileId, seller.id, shouldCheckEligibility]);`,
    "seller review eligibility guard",
  );
  source = replaceExact(
    source,
    `  async function submitReview(event: FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    if (eligibilityState !== \"eligible\" || reviewInFlightRef.current) return;\n    reviewInFlightRef.current = true;\n    setNotice(\"\");\n    setSaving(true);\n    try {\n      const result = await createSellerReview({\n        sellerUserId: seller.id,\n        reviewerUserId: auth.profile?.id ?? null,\n        rating,\n        comment,\n        traits: selectedTraits,\n      });\n      if (result.ok) {\n        setComment(\"\");\n        setRating(5);\n        setSelectedTraits([]);\n        setEligibilityState(\"existing_review\");\n        setNotice(\n          text(\n            \"تم إرسال التقييم للمراجعة قبل ظهوره للعامة.\",\n            \"Review submitted for moderation before public display.\",\n          ),\n        );\n      } else {\n        setNotice(result.error.message);\n        if (result.error.code === \"permission_denied\") {\n          setEligibilityState(\"no_qualifying_interaction\");\n        } else if (result.error.code === \"status_mismatch\") {\n          setEligibilityState(\"existing_review\");\n        }\n      }\n    } finally {\n      reviewInFlightRef.current = false;\n      setSaving(false);\n    }\n  }`,
    `  async function submitReview(event: FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    const currentProfileId = profileId;\n    if (eligibilityState !== \"eligible\" || !currentProfileId) return;\n    if (reviewSubmitProfilesRef.current.has(currentProfileId)) return;\n\n    const currentRating = rating;\n    const currentComment = comment;\n    const currentTraits = selectedTraits;\n    reviewSubmitProfilesRef.current.add(currentProfileId);\n    setNotice(\"\");\n    setSaving(true);\n    try {\n      const result = await createSellerReview({\n        sellerUserId: seller.id,\n        reviewerUserId: currentProfileId,\n        rating: currentRating,\n        comment: currentComment,\n        traits: currentTraits,\n      });\n      if (currentProfileId !== profileIdRef.current) return;\n      if (result.ok) {\n        setComment(\"\");\n        setRating(5);\n        setSelectedTraits([]);\n        setEligibilityState(\"existing_review\");\n        setNotice(\n          text(\n            \"تم إرسال التقييم للمراجعة قبل ظهوره للعامة.\",\n            \"Review submitted for moderation before public display.\",\n          ),\n        );\n      } else {\n        setNotice(result.error.message);\n        if (result.error.code === \"permission_denied\") {\n          setEligibilityState(\"no_qualifying_interaction\");\n        } else if (result.error.code === \"status_mismatch\") {\n          setEligibilityState(\"existing_review\");\n        }\n      }\n    } finally {\n      reviewSubmitProfilesRef.current.delete(currentProfileId);\n      if (currentProfileId === profileIdRef.current) setSaving(false);\n    }\n  }`,
    "seller review submit isolation",
  );
  source = replaceExact(source, `<SellerReviewCard key={review.id} review={review} canManageResponse={isOwnProfile} />`, `<SellerReviewCard\n                key={\`${profileId ?? \"signed-out\"}:\${review.id}\`}\n                review={review}\n                canManageResponse={isOwnProfile}\n              />`, "seller review account key");
  return source;
});

await patchFile("src/routes/profile.tsx", (source) => {
  source = replaceExact(
    source,
    `  const verificationRequestIdRef = useRef(0);\n  const profileId = auth.profile?.id ?? null;`,
    `  const verificationRequestIdRef = useRef(0);\n  const loadedProfileIdRef = useRef<string | null>(null);\n  const profileId = auth.profile?.id ?? null;\n  const profileIdRef = useRef<string | null>(profileId);\n  const settingsSavingProfilesRef = useRef<Set<string>>(new Set());\n  const passwordSavingProfilesRef = useRef<Set<string>>(new Set());\n  const deletionSavingProfilesRef = useRef<Set<string>>(new Set());\n  const mediaSavingProfilesRef = useRef<Set<string>>(new Set());\n  profileIdRef.current = profileId;`,
    "profile action refs",
  );
  source = replaceCount(source, "currentProfileId !== auth.profile?.id", "currentProfileId !== profileIdRef.current", 2, "profile load guards");
  source = replaceExact(
    source,
    `  async function handleChangePassword(event: FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    setPasswordNotice(\"\");\n    if (newPassword !== confirmPassword) {\n      setPasswordNotice(text(\"كلمتا المرور غير متطابقتين.\", \"Passwords do not match.\"));\n      return;\n    }\n\n    setPasswordSaving(true);\n    const result = await changeOwnPassword(profileId, newPassword);\n    setPasswordSaving(false);\n    if (!result.ok) {\n      setPasswordNotice(result.error.message);\n      return;\n    }\n    setNewPassword(\"\");\n    setConfirmPassword(\"\");\n    setPasswordNotice(text(\"تم تغيير كلمة المرور بنجاح.\", \"Password changed successfully.\"));\n  }`,
    `  async function handleChangePassword(event: FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    const currentProfileId = profileId;\n    if (!currentProfileId || passwordSavingProfilesRef.current.has(currentProfileId)) return;\n\n    setPasswordNotice(\"\");\n    if (newPassword !== confirmPassword) {\n      setPasswordNotice(text(\"كلمتا المرور غير متطابقتين.\", \"Passwords do not match.\"));\n      return;\n    }\n\n    const currentPassword = newPassword;\n    passwordSavingProfilesRef.current.add(currentProfileId);\n    setPasswordSaving(true);\n    try {\n      const result = await changeOwnPassword(currentProfileId, currentPassword);\n      if (currentProfileId !== profileIdRef.current) return;\n      if (!result.ok) {\n        setPasswordNotice(result.error.message);\n        return;\n      }\n      setNewPassword(\"\");\n      setConfirmPassword(\"\");\n      setPasswordNotice(\n        text(\"تم تغيير كلمة المرور بنجاح.\", \"Password changed successfully.\"),\n      );\n    } finally {\n      passwordSavingProfilesRef.current.delete(currentProfileId);\n      if (currentProfileId === profileIdRef.current) setPasswordSaving(false);\n    }\n  }`,
    "password action isolation",
  );
  source = replaceExact(
    source,
    `  async function handleAccountDeletionRequest() {\n    setDeletionSaving(true);\n    setDeletionNotice(\"\");\n    const result = await createAccountDeletionRequest(profileId);\n    setDeletionSaving(false);\n    if (!result.ok) {\n      setDeletionNotice(result.error.message);\n      return;\n    }\n    setDeleteConfirmOpen(false);\n    setDeletionNotice(\n      text(\n        \"تم تسجيل طلب حذف الحساب. ستراجعه الإدارة قبل تنفيذ الحذف الآمن.\",\n        \"Your account deletion request was recorded and will be reviewed before secure deletion.\",\n      ),\n    );\n  }`,
    `  async function handleAccountDeletionRequest() {\n    const currentProfileId = profileId;\n    if (!currentProfileId || deletionSavingProfilesRef.current.has(currentProfileId)) return;\n\n    deletionSavingProfilesRef.current.add(currentProfileId);\n    setDeletionSaving(true);\n    setDeletionNotice(\"\");\n    try {\n      const result = await createAccountDeletionRequest(currentProfileId);\n      if (currentProfileId !== profileIdRef.current) return;\n      if (!result.ok) {\n        setDeletionNotice(result.error.message);\n        return;\n      }\n      setDeleteConfirmOpen(false);\n      setDeletionNotice(\n        text(\n          \"تم تسجيل طلب حذف الحساب. ستراجعه الإدارة قبل تنفيذ الحذف الآمن.\",\n          \"Your account deletion request was recorded and will be reviewed before secure deletion.\",\n        ),\n      );\n    } finally {\n      deletionSavingProfilesRef.current.delete(currentProfileId);\n      if (currentProfileId === profileIdRef.current) setDeletionSaving(false);\n    }\n  }`,
    "deletion action isolation",
  );
  source = replaceExact(
    source,
    `  async function handleSaveProfileBasics(event: FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    setSettingsNotice(\"\");\n    setSettingsSaving(true);\n    const result = await updateOwnProfileBasics(profileId, {\n      firstName: settingsFirstName,\n      lastName: settingsLastName,\n      displayName: settingsDisplayName || null,\n      governorate: settingsGovernorate || null,\n      cityArea: settingsCityArea || null,\n      bio: settingsBio || null,\n      businessName: settingsBusinessName || null,\n      phone: settingsPhone || null,\n      whatsapp: settingsWhatsapp || null,\n      preferredContactMethod: settingsPreferredContact || null,\n    });\n    setSettingsSaving(false);\n    if (!result.ok) {\n      setSettingsNotice(result.error.message);\n      return;\n    }\n    const refreshResult = await auth.refreshProfile();\n    setSettingsNotice(\n      refreshResult.error\n        ? text(\n            \"تم الحفظ، لكن تعذر تحديث العرض فوراً. أعد فتح الصفحة عند الحاجة.\",\n            \"Saved, but the view could not refresh immediately. Reopen the page if needed.\",\n          )\n        : text(\"تم حفظ معلومات الحساب وتحديثها.\", \"Account information saved and refreshed.\"),\n    );\n  }`,
    `  async function handleSaveProfileBasics(event: FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    const currentProfileId = profileId;\n    if (!currentProfileId || settingsSavingProfilesRef.current.has(currentProfileId)) return;\n\n    const payload = {\n      firstName: settingsFirstName,\n      lastName: settingsLastName,\n      displayName: settingsDisplayName || null,\n      governorate: settingsGovernorate || null,\n      cityArea: settingsCityArea || null,\n      bio: settingsBio || null,\n      businessName: settingsBusinessName || null,\n      phone: settingsPhone || null,\n      whatsapp: settingsWhatsapp || null,\n      preferredContactMethod: settingsPreferredContact || null,\n    };\n    settingsSavingProfilesRef.current.add(currentProfileId);\n    setSettingsNotice(\"\");\n    setSettingsSaving(true);\n    try {\n      const result = await updateOwnProfileBasics(currentProfileId, payload);\n      if (currentProfileId !== profileIdRef.current) return;\n      if (!result.ok) {\n        setSettingsNotice(result.error.message);\n        return;\n      }\n      const refreshResult = await auth.refreshProfile();\n      if (currentProfileId !== profileIdRef.current) return;\n      setSettingsNotice(\n        refreshResult.error\n          ? text(\n              \"تم الحفظ، لكن تعذر تحديث العرض فوراً. أعد فتح الصفحة عند الحاجة.\",\n              \"Saved, but the view could not refresh immediately. Reopen the page if needed.\",\n            )\n          : text(\n              \"تم حفظ معلومات الحساب وتحديثها.\",\n              \"Account information saved and refreshed.\",\n            ),\n      );\n    } finally {\n      settingsSavingProfilesRef.current.delete(currentProfileId);\n      if (currentProfileId === profileIdRef.current) setSettingsSaving(false);\n    }\n  }`,
    "profile basics isolation",
  );
  source = replaceExact(
    source,
    `  async function handleProfileMedia(kind: \"avatar\" | \"cover\", file: File | undefined) {\n    if (!file) return;\n    setSettingsNotice(\"\");\n    setMediaSaving(kind);\n    const result = await uploadProfileMedia({\n      userId: profileId,\n      kind,\n      file,\n      oldPath: kind === \"avatar\" ? auth.profile?.avatarPath : auth.profile?.coverPath,\n    });\n    setMediaSaving(null);\n    if (!result.ok) {\n      setSettingsNotice(result.error.message);\n      return;\n    }\n    const refreshResult = await auth.refreshProfile();\n    setSettingsNotice(\n      refreshResult.error\n        ? text(\n            \"تم حفظ الصورة، لكن تعذر تحديث العرض فوراً.\",\n            \"Image saved, but the view could not refresh immediately.\",\n          )\n        : text(\"تم حفظ الصورة وتحديث الحساب.\", \"Image saved and account refreshed.\"),\n    );\n  }`,
    `  async function handleProfileMedia(kind: \"avatar\" | \"cover\", file: File | undefined) {\n    const currentProfileId = profileId;\n    if (!file || !currentProfileId || mediaSavingProfilesRef.current.has(currentProfileId)) return;\n\n    const oldPath = kind === \"avatar\" ? auth.profile?.avatarPath : auth.profile?.coverPath;\n    mediaSavingProfilesRef.current.add(currentProfileId);\n    setSettingsNotice(\"\");\n    setMediaSaving(kind);\n    try {\n      const result = await uploadProfileMedia({\n        userId: currentProfileId,\n        kind,\n        file,\n        oldPath,\n      });\n      if (currentProfileId !== profileIdRef.current) return;\n      if (!result.ok) {\n        setSettingsNotice(result.error.message);\n        return;\n      }\n      const refreshResult = await auth.refreshProfile();\n      if (currentProfileId !== profileIdRef.current) return;\n      setSettingsNotice(\n        refreshResult.error\n          ? text(\n              \"تم حفظ الصورة، لكن تعذر تحديث العرض فوراً.\",\n              \"Image saved, but the view could not refresh immediately.\",\n            )\n          : text(\"تم حفظ الصورة وتحديث الحساب.\", \"Image saved and account refreshed.\"),\n      );\n    } finally {\n      mediaSavingProfilesRef.current.delete(currentProfileId);\n      if (currentProfileId === profileIdRef.current) setMediaSaving(null);\n    }\n  }`,
    "profile media upload isolation",
  );
  source = replaceExact(
    source,
    `  async function handleRemoveProfileMedia(kind: \"avatar\" | \"cover\") {\n    setSettingsNotice(\"\");\n    setMediaSaving(kind);\n    const result = await removeProfileMedia(\n      profileId,\n      kind,\n      kind === \"avatar\" ? auth.profile?.avatarPath : auth.profile?.coverPath,\n    );\n    setMediaSaving(null);\n    if (!result.ok) {\n      setSettingsNotice(result.error.message);\n      return;\n    }\n    const refreshResult = await auth.refreshProfile();\n    setSettingsNotice(\n      refreshResult.error\n        ? text(\n            \"تمت إزالة الصورة، لكن تعذر تحديث العرض فوراً.\",\n            \"Image removed, but the view could not refresh immediately.\",\n          )\n        : text(\"تمت إزالة الصورة وتحديث الحساب.\", \"Image removed and account refreshed.\"),\n    );\n  }`,
    `  async function handleRemoveProfileMedia(kind: \"avatar\" | \"cover\") {\n    const currentProfileId = profileId;\n    if (!currentProfileId || mediaSavingProfilesRef.current.has(currentProfileId)) return;\n\n    const oldPath = kind === \"avatar\" ? auth.profile?.avatarPath : auth.profile?.coverPath;\n    mediaSavingProfilesRef.current.add(currentProfileId);\n    setSettingsNotice(\"\");\n    setMediaSaving(kind);\n    try {\n      const result = await removeProfileMedia(currentProfileId, kind, oldPath);\n      if (currentProfileId !== profileIdRef.current) return;\n      if (!result.ok) {\n        setSettingsNotice(result.error.message);\n        return;\n      }\n      const refreshResult = await auth.refreshProfile();\n      if (currentProfileId !== profileIdRef.current) return;\n      setSettingsNotice(\n        refreshResult.error\n          ? text(\n              \"تمت إزالة الصورة، لكن تعذر تحديث العرض فوراً.\",\n              \"Image removed, but the view could not refresh immediately.\",\n            )\n          : text(\n              \"تمت إزالة الصورة وتحديث الحساب.\",\n              \"Image removed and account refreshed.\",\n            ),\n      );\n    } finally {\n      mediaSavingProfilesRef.current.delete(currentProfileId);\n      if (currentProfileId === profileIdRef.current) setMediaSaving(null);\n    }\n  }`,
    "profile media removal isolation",
  );
  source = replaceExact(
    source,
    `  useEffect(() => {\n    if (auth.status !== \"signedIn\") return;\n    setSettingsDisplayName(auth.profile?.displayName ?? \"\");\n    setSettingsFirstName(\n      auth.profile?.firstName ?? auth.profile?.displayName?.split(\" \").filter(Boolean).at(0) ?? \"\",\n    );\n    setSettingsLastName(\n      auth.profile?.lastName ??\n        auth.profile?.displayName?.split(\" \").filter(Boolean).slice(1).join(\" \") ??\n        \"\",\n    );\n    setSettingsGovernorate(auth.profile?.governorate ?? \"\");\n    setSettingsCityArea(auth.profile?.cityArea ?? \"\");\n    setSettingsBio(auth.profile?.bio ?? \"\");\n    setSettingsBusinessName(auth.profile?.businessName ?? \"\");\n    setSettingsPhone(auth.profile?.phone ?? \"\");\n    setSettingsWhatsapp(auth.profile?.whatsapp ?? \"\");\n    setSettingsPreferredContact(auth.profile?.preferredContactMethod ?? \"\");\n  }, [auth.profile, auth.status]);`,
    `  useEffect(() => {\n    if (auth.status !== \"signedIn\" || !profileId) {\n      loadedProfileIdRef.current = null;\n      setSettingsDisplayName(\"\");\n      setSettingsFirstName(\"\");\n      setSettingsLastName(\"\");\n      setSettingsGovernorate(\"\");\n      setSettingsCityArea(\"\");\n      setSettingsBio(\"\");\n      setSettingsBusinessName(\"\");\n      setSettingsPhone(\"\");\n      setSettingsWhatsapp(\"\");\n      setSettingsPreferredContact(\"\");\n      setSettingsSaving(false);\n      setSettingsNotice(\"\");\n      setNewPassword(\"\");\n      setConfirmPassword(\"\");\n      setPasswordSaving(false);\n      setPasswordNotice(\"\");\n      setDeleteConfirmOpen(false);\n      setDeletionSaving(false);\n      setDeletionNotice(\"\");\n      setMediaSaving(null);\n      return;\n    }\n\n    const accountChanged = loadedProfileIdRef.current !== profileId;\n    loadedProfileIdRef.current = profileId;\n    setSettingsDisplayName(auth.profile?.displayName ?? \"\");\n    setSettingsFirstName(\n      auth.profile?.firstName ?? auth.profile?.displayName?.split(\" \").filter(Boolean).at(0) ?? \"\",\n    );\n    setSettingsLastName(\n      auth.profile?.lastName ??\n        auth.profile?.displayName?.split(\" \").filter(Boolean).slice(1).join(\" \") ??\n        \"\",\n    );\n    setSettingsGovernorate(auth.profile?.governorate ?? \"\");\n    setSettingsCityArea(auth.profile?.cityArea ?? \"\");\n    setSettingsBio(auth.profile?.bio ?? \"\");\n    setSettingsBusinessName(auth.profile?.businessName ?? \"\");\n    setSettingsPhone(auth.profile?.phone ?? \"\");\n    setSettingsWhatsapp(auth.profile?.whatsapp ?? \"\");\n    setSettingsPreferredContact(auth.profile?.preferredContactMethod ?? \"\");\n    if (accountChanged) {\n      setSettingsSaving(false);\n      setSettingsNotice(\"\");\n      setNewPassword(\"\");\n      setConfirmPassword(\"\");\n      setPasswordSaving(false);\n      setPasswordNotice(\"\");\n      setDeleteConfirmOpen(false);\n      setDeletionSaving(false);\n      setDeletionNotice(\"\");\n      setMediaSaving(null);\n    }\n  }, [auth.profile, auth.status, profileId]);`,
    "profile account transition reset",
  );
  return source;
});

const regressionTest = `import assert from "node:assert/strict";\nimport { readFile } from "node:fs/promises";\nimport test from "node:test";\n\nconst [favorites, savedSearches, ownerListings, reviewCard, sellerRoute, profile, offers, packageSource] =\n  await Promise.all([\n    readFile(new URL("../src/routes/favorites.tsx", import.meta.url), "utf8"),\n    readFile(new URL("../src/routes/saved-searches.tsx", import.meta.url), "utf8"),\n    readFile(new URL("../src/routes/profile/listings.tsx", import.meta.url), "utf8"),\n    readFile(new URL("../src/features/reviews/SellerReviewCard.tsx", import.meta.url), "utf8"),\n    readFile(new URL("../src/routes/seller.$id.tsx", import.meta.url), "utf8"),\n    readFile(new URL("../src/routes/profile.tsx", import.meta.url), "utf8"),\n    readFile(new URL("../src/routes/offers.tsx", import.meta.url), "utf8"),\n    readFile(new URL("../package.json", import.meta.url), "utf8"),\n  ]);\n\ntest("favorites reject stale removals with account-scoped locks", () => {\n  assert.match(favorites, /profileIdRef = useRef<string \\| null>/);\n  assert.match(favorites, /const scopeKey = \\[currentProfileId, listingId\\]\\.join\\(\":\"\\)/);\n  assert.match(favorites, /currentProfileId !== profileIdRef\\.current/);\n  assert.match(favorites, /removeInFlightRef\\.current\\.delete\\(scopeKey\\)/);\n});\n\ntest("saved searches reset local state and isolate every account mutation", () => {\n  assert.match(savedSearches, /loadedProfileIdRef = useRef<string \\| null>/);\n  assert.match(savedSearches, /setLocalItems\\(\\[\\]\\)/);\n  assert.match(savedSearches, /creatingSearchProfilesRef = useRef<Set<string>>/);\n  assert.match(savedSearches, /frequencyScopesRef = useRef<Set<string>>/);\n  assert.match(savedSearches, /deletingSearchScopesRef = useRef<Set<string>>/);\n  assert.match(savedSearches, /currentProfileId !== profileIdRef\\.current/);\n});\n\ntest("owner listing results are scoped to the initiating account", () => {\n  assert.match(ownerListings, /actionProfileId !== profileIdRef\\.current/);\n  assert.match(ownerListings, /key=\\{\\`\\$\\{profileId \\?\\? \"signed-out\"\\}:\\$\\{listing\\.id\\}\\`\\}/);\n  assert.match(ownerListings, /onDeleted\\(userId, listing\\.id\\)/);\n  assert.match(ownerListings, /onChanged\\(userId, result\\.data\\)/);\n});\n\ntest("seller review actions and forms reject replacement accounts", () => {\n  assert.match(reviewCard, /responseScopesRef = useRef<Set<string>>/);\n  assert.match(reviewCard, /reportScopesRef = useRef<Set<string>>/);\n  assert.match(reviewCard, /currentProfileId !== profileIdRef\\.current/);\n  assert.match(sellerRoute, /reviewSubmitProfilesRef = useRef<Set<string>>/);\n  assert.match(sellerRoute, /reviewerUserId: currentProfileId/);\n  assert.match(sellerRoute, /currentProfileId !== profileIdRef\\.current/);\n});\n\ntest("profile mutations and refreshes remain bound to one account", () => {\n  assert.match(profile, /settingsSavingProfilesRef = useRef<Set<string>>/);\n  assert.match(profile, /passwordSavingProfilesRef = useRef<Set<string>>/);\n  assert.match(profile, /deletionSavingProfilesRef = useRef<Set<string>>/);\n  assert.match(profile, /mediaSavingProfilesRef = useRef<Set<string>>/);\n  assert.match(profile, /if \\(currentProfileId !== profileIdRef\\.current\\) return;/);\n  assert.match(profile, /loadedProfileIdRef\\.current = null/);\n});\n\ntest("offers stay public and outside account isolation state", () => {\n  assert.doesNotMatch(offers, /useAuth/);\n  assert.match(offers, /fetchActivePriceDropOffers/);\n});\n\ntest("account surface isolation remains in the permanent activity contract", () => {\n  const packageJson = JSON.parse(packageSource);\n  assert.match(\n    packageJson.scripts["test:activity-center"],\n    /account-surface-action-isolation\\.test\\.mjs/,\n  );\n  assert.match(packageJson.scripts.check, /npm run test:activity-center/);\n});\n`;
await writeFile("scripts/account-surface-action-isolation.test.mjs", regressionTest);

await patchFile("package.json", (source) => {
  const packageJson = JSON.parse(source);
  const script = packageJson.scripts["test:activity-center"];
  const testFile = "scripts/account-surface-action-isolation.test.mjs";
  if (script.includes(testFile)) throw new Error("Regression test already registered");
  packageJson.scripts["test:activity-center"] = `${script} ${testFile}`;
  return `${JSON.stringify(packageJson, null, 2)}\n`;
});

console.log("Applied account-surface stale-result isolation patch.");
