name: Format saved search recovery

on:
  push:
    branches:
      - feature/recover-saved-search-loads

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/recover-saved-search-loads
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Format changed files
        run: npx eslint --fix src/routes/saved-searches.tsx scripts/saved-search-background-alerts.test.mjs
      - name: Commit formatting and remove formatter
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/format-saved-search-recovery.yml
          git add src/routes/saved-searches.tsx scripts/saved-search-background-alerts.test.mjs
          git commit -m "Format saved search recovery files"
          git push origin HEAD:feature/recover-saved-search-loads
