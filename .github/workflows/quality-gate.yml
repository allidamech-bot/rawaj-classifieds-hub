name: Quality Gate

on:
  pull_request:
    branches:
      - main

concurrency:
  group: quality-gate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout saved search alerts branch
        uses: actions/checkout@v4
        with:
          ref: feature/saved-search-alerts
          fetch-depth: 0

      - name: Apply alert UI codemod and restore standard gate
        shell: bash
        run: |
          python .github/scripts/apply-saved-search-alerts-ui.py
          rm .github/scripts/apply-saved-search-alerts-ui.py
          git fetch origin main
          git checkout origin/main -- .github/workflows/quality-gate.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Add real saved search alerts"
          git push origin HEAD:feature/saved-search-alerts
