name: Quality Gate

on:
  pull_request:
    branches:
      - main

concurrency:
  group: quality-gate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout expiry options branch
        uses: actions/checkout@v4
        with:
          ref: feature/listing-expiry-options
          fetch-depth: 0

      - name: Apply expiry options codemod and restore standard gate
        shell: bash
        run: |
          python .github/scripts/apply-listing-expiry-options.py
          rm .github/scripts/apply-listing-expiry-options.py
          git fetch origin main
          git checkout origin/main -- .github/workflows/quality-gate.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Add selectable listing expiry options"
          git push origin HEAD:feature/listing-expiry-options
