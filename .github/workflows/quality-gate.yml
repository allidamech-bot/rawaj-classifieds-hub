name: Quality Gate

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

concurrency:
  group: quality-gate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Format seller review response diagnostic
        run: |
          npx prettier --write src/lib/api/reviews.ts scripts/seller-review-response.test.mjs
          git diff -- src/lib/api/reviews.ts scripts/seller-review-response.test.mjs > /tmp/review-response-prettier.diff

      - name: Upload seller review response formatting artifact
        uses: actions/upload-artifact@v4
        with:
          name: seller-review-response-formatting
          path: |
            src/lib/api/reviews.ts
            scripts/seller-review-response.test.mjs
            /tmp/review-response-prettier.diff
          if-no-files-found: error

      - name: Require committed seller review response formatting
        run: git diff --exit-code -- src/lib/api/reviews.ts scripts/seller-review-response.test.mjs

      - name: Lint changed source files
        shell: bash
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: |
          base="$BASE_SHA"
          if [ -z "$base" ] || [ "$base" = "0000000000000000000000000000000000000000" ]; then
            base="$(git rev-parse HEAD^)"
          fi

          mapfile -t files < <(
            git diff --name-only --diff-filter=ACMR "$base" "$GITHUB_SHA" -- \
              '*.js' '*.jsx' '*.ts' '*.tsx' '*.mjs' '*.cjs'
          )

          if [ "${#files[@]}" -eq 0 ]; then
            echo "No changed JS/TS source files to lint."
            exit 0
          fi

          printf 'Linting %s\n' "${files[@]}"
          npx eslint --quiet "${files[@]}"

      - name: Syria location classification contract
        run: npm run test:location-classification

      - name: Syria location discovery and presentation contract
        run: npm run test:location-presentation

      - name: Listings mobile category switch contract
        run: npm run test:listings-filters

      - name: Seller review eligibility contract
        run: npm run test:review-eligibility

      - name: Seller review response contract
        run: npm run test:review-response

      - name: Typecheck
        run: npm run typecheck -- --pretty false

      - name: Listing system regression contract
        run: npm run test:listing-system

      - name: Production build
        run: npm run build
