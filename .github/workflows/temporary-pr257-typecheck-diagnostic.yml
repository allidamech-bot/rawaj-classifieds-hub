name: Temporary PR 257 Typecheck Diagnostic

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Capture typecheck output
        run: npm run typecheck -- --pretty false > typecheck.log 2>&1 || true
      - uses: actions/upload-artifact@v4
        with:
          name: pr257-typecheck-diagnostic
          path: typecheck.log
          retention-days: 1
