name: Admin Safety Operation Integrity

on:
  pull_request:
    paths:
      - "src/lib/api/safety-cases.ts"
      - "src/routes/admin.tsx"
      - "src/routes/admin.safety.tsx"
      - "scripts/admin-safety-operation-integrity.test.mjs"
      - ".github/workflows/admin-safety-operation-integrity.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Admin safety operation integrity contract
        run: node --test scripts/admin-safety-operation-integrity.test.mjs
      - name: Lint changed TypeScript
        run: npx eslint --quiet src/lib/api/safety-cases.ts src/routes/admin.tsx
