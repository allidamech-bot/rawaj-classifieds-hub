name: Apply Admin Campaign Notice Format

on:
  pull_request:
    branches:
      - main
    paths:
      - "scripts/apply-admin-campaign-notice-format.mjs"
      - ".github/workflows/apply-admin-campaign-notice-format.yml"

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/admin-actions-integrity-audit
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/apply-admin-campaign-notice-format.mjs
      - name: Commit campaign notice formatting repair
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/routes/admin.campaigns.tsx scripts/apply-admin-campaign-notice-format.mjs
          git commit -m "style: format campaign status notice"
          git push origin HEAD:agent/admin-actions-integrity-audit
