name: Notification Actions Runtime Integrity

on:
  pull_request:
    paths:
      - "src/routes/notifications.tsx"
      - "scripts/notification-actions-runtime-integrity.test.mjs"
      - ".github/workflows/notification-actions-runtime-integrity.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: notification-actions-runtime-integrity-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node --test scripts/notification-actions-runtime-integrity.test.mjs
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build
