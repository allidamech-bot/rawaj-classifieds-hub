name: Saved Items Actions Runtime Integrity

on:
  pull_request:
    paths:
      - "src/routes/favorites.tsx"
      - "src/routes/saved-searches.tsx"
      - "src/features/favorites/FavoriteListingCard.tsx"
      - "scripts/saved-items-actions-runtime-integrity.test.mjs"
      - ".github/workflows/saved-items-actions-runtime-integrity.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: saved-items-actions-runtime-integrity-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node --test scripts/saved-items-actions-runtime-integrity.test.mjs
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build
