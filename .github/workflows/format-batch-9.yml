name: Format Batch 9

on:
  push:
    branches:
      - agent/launch-readiness-batch-9

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/launch-readiness-batch-9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prettier --write e2e/marketplace-smoke.spec.ts e2e/production-smoke.spec.ts scripts/production-deployment-truth.test.mjs .github/workflows/production-smoke.yml
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add e2e/marketplace-smoke.spec.ts e2e/production-smoke.spec.ts scripts/production-deployment-truth.test.mjs .github/workflows/production-smoke.yml
          git commit -m "Format Batch 9 production smoke files"
          git push origin HEAD:agent/launch-readiness-batch-9
