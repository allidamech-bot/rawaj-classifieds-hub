name: Apply Profile Seller Actions Integrity

on:
  pull_request:
    branches:
      - main
    paths:
      - "scripts/apply-profile-seller-actions-integrity.mjs"
      - "scripts/fix-profile-seller-generator.mjs"
      - ".github/workflows/apply-profile-seller-actions-integrity.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: apply-profile-seller-actions-integrity
  cancel-in-progress: false

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/full-site-actions-integrity-audit
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply guarded profile seller repair
        id: repair
        continue-on-error: true
        shell: bash
        run: |
          node scripts/fix-profile-seller-generator.mjs > profile-seller-repair.log 2>&1
          node scripts/apply-profile-seller-actions-integrity.mjs >> profile-seller-repair.log 2>&1
      - name: Upload repair diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: profile-seller-repair-${{ github.sha }}
          path: profile-seller-repair.log
          if-no-files-found: warn
          retention-days: 7
      - name: Fail when repair failed
        if: steps.repair.outcome == 'failure'
        run: exit 1
      - name: Commit repair
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/routes/profile.tsx src/routes/seller.\$id.tsx scripts/apply-profile-seller-actions-integrity.mjs scripts/fix-profile-seller-generator.mjs
          git commit -m "fix: harden profile and seller actions"
          git push origin HEAD:agent/full-site-actions-integrity-audit
